<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>{{title}}</title>
    <link rel='stylesheet' href='/main.css'>
  </head>
  <body>
    <h1>Rate Your Favorite Cupcakes!</h1>
      <div class='cupcakesList'>
        {{#each cupcakes}}
        <a href='/cupcakes/{{this.id}}'>
          <div class='cupcake'>
            <h3>{{this.flavor}}</h3>
            <p>{{this.description}}</p>
            <p>{{this.baker}}</p>
            <p>{{this.averageRating}}</p>
          </div>
        {{/each}}
    </div>
  </body>
</html>
